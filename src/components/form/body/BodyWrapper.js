import styled from '@emotion/styled';

export const BodyWrapper = styled.main`
  // position: relative;
  // left: 2rem;
  width: fit-content;
  height: fit-content;
  display: grid;
  margin-right: auto;
  margin-left: auto;
  grid-template-columns: 1fr 1fr;
  margin-top: 2rem;
  margin-bottom: 3rem;
`;
