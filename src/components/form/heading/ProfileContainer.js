import styled from '@emotion/styled';

export const ProfileContainer = styled.div`
  position: relative;
  z-index: 99;
  left: -13%;
  margin-top: 0.5rem;
  margin-bottom: 0.5em;
  width: fit-content;
`;
