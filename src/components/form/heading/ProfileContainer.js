import styled from '@emotion/styled';

export const ProfileContainer = styled.div`
  position: relative;
  z-index: 99;
  left: -13%;
  margin-top: auto;
  margin-bottom: auto;
  width: fit-content;
`;
