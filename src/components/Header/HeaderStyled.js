import styled from '@emotion/styled';

export const HeaderStyled = styled.div`
  justify-content: space-around;
  width: 100vw;
  height: min(9rem, 15vh);
  position: fixed;
  border: 1px solid red;
`;
